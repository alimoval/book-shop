<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule (.*) http://rest/server/$1 [L] 

       # If the requested filename exists, simply serve it.
    # We only want to let Apache serve files and not directories.
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule .? - [L]

</IfModule>